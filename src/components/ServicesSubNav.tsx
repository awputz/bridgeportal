import { Link, useLocation } from "react-router-dom";
import { NAV_ITEMS } from "@/lib/constants";
import { SwipeHint } from "@/components/SwipeHint";
export const ServicesSubNav = () => {
  const location = useLocation();
  return <section className="py-2 sm:py-3 md:py-4 border-b border-white/5 bg-white/[0.02] sticky top-[calc(4rem+0.75rem)] sm:top-[calc(4.5rem+0.75rem)] md:top-[calc(5rem+1rem)] lg:top-[calc(4.5rem+1.5rem)] z-40 backdrop-blur-xl">
      
    </section>;
};