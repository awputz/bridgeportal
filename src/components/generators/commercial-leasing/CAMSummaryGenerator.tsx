import { useState } from "react"; import { Receipt } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const CAMSummaryGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ property: "", camDetails: "" }); const promptBuilder = (data: Record<string, any>) => `Explain CAM charges for ${data.property}. Details: ${data.camDetails}`; return (<GeneratorShell id="cam-summary" title="CAM Summary" description="Explain CAM charges to tenants in plain language" icon={Receipt} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.property}><div className="space-y-4"><div className="space-y-2"><Label>Property *</Label><Input value={formData.property} onChange={(e) => setFormData({ ...formData, property: e.target.value })} /></div><div className="space-y-2"><Label>CAM Details</Label><Textarea value={formData.camDetails} onChange={(e) => setFormData({ ...formData, camDetails: e.target.value })} rows={4} /></div></div></GeneratorShell>); };
export default CAMSummaryGenerator;
