import { useState } from "react"; import { Megaphone } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const LandlordPitchGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ landlordName: "", property: "" }); const promptBuilder = (data: Record<string, any>) => `Write a pitch letter to ${data.landlordName} for ${data.property}`; return (<GeneratorShell id="landlord-pitch" title="Landlord Pitch Letter" description="Pitch letters to acquire exclusive listings" icon={Megaphone} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!(formData.landlordName && formData.property)}><div className="space-y-4"><div className="space-y-2"><Label>Landlord Name *</Label><Input value={formData.landlordName} onChange={(e) => setFormData({ ...formData, landlordName: e.target.value })} /></div><div className="space-y-2"><Label>Property *</Label><Input value={formData.property} onChange={(e) => setFormData({ ...formData, property: e.target.value })} /></div></div></GeneratorShell>); };
export default LandlordPitchGenerator;
