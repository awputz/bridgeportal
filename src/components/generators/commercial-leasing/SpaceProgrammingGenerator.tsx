import { useState } from "react"; import { Layers } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const SpaceProgrammingGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ companyName: "", headcount: "", requirements: "" }); const promptBuilder = (data: Record<string, any>) => `Create space programming document for ${data.companyName}. Headcount: ${data.headcount}. Requirements: ${data.requirements}`; return (<GeneratorShell id="space-programming" title="Space Programming" description="Help tenants define their space needs and requirements" icon={Layers} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.companyName}><div className="space-y-4"><div className="space-y-2"><Label>Company Name *</Label><Input value={formData.companyName} onChange={(e) => setFormData({ ...formData, companyName: e.target.value })} /></div><div className="space-y-2"><Label>Headcount</Label><Input value={formData.headcount} onChange={(e) => setFormData({ ...formData, headcount: e.target.value })} /></div><div className="space-y-2"><Label>Requirements</Label><Textarea value={formData.requirements} onChange={(e) => setFormData({ ...formData, requirements: e.target.value })} rows={4} /></div></div></GeneratorShell>); };
export default SpaceProgrammingGenerator;
