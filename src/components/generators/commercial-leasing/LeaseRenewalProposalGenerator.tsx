import { useState } from "react"; import { Handshake } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const LeaseRenewalProposalGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ tenantName: "", currentTerms: "", proposedTerms: "" }); const promptBuilder = (data: Record<string, any>) => `Create lease renewal proposal for ${data.tenantName}. Current: ${data.currentTerms}. Proposed: ${data.proposedTerms}`; return (<GeneratorShell id="lease-renewal-proposal" title="Lease Renewal Proposal" description="Renewal negotiation documents and proposals" icon={Handshake} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.tenantName}><div className="space-y-4"><div className="space-y-2"><Label>Tenant Name *</Label><Input value={formData.tenantName} onChange={(e) => setFormData({ ...formData, tenantName: e.target.value })} /></div><div className="space-y-2"><Label>Current Terms</Label><Textarea value={formData.currentTerms} onChange={(e) => setFormData({ ...formData, currentTerms: e.target.value })} rows={3} /></div><div className="space-y-2"><Label>Proposed Terms</Label><Textarea value={formData.proposedTerms} onChange={(e) => setFormData({ ...formData, proposedTerms: e.target.value })} rows={3} /></div></div></GeneratorShell>); };
export default LeaseRenewalProposalGenerator;
