import { useState } from "react"; import { FileText } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const TenantProposalGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ tenantName: "", requirements: "" }); const promptBuilder = (data: Record<string, any>) => `Create a tenant proposal for ${data.tenantName}. Requirements: ${data.requirements}`; return (<GeneratorShell id="tenant-proposal" title="Tenant Proposal" description="RFP responses and tenant requirement packages" icon={FileText} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.tenantName}><div className="space-y-4"><div className="space-y-2"><Label>Tenant Name *</Label><Input value={formData.tenantName} onChange={(e) => setFormData({ ...formData, tenantName: e.target.value })} /></div><div className="space-y-2"><Label>Requirements</Label><Textarea value={formData.requirements} onChange={(e) => setFormData({ ...formData, requirements: e.target.value })} rows={4} /></div></div></GeneratorShell>); };
export default TenantProposalGenerator;
