import { useState } from "react"; import { FileSearch } from "lucide-react"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const LeaseAbstractGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ leaseTerms: "" }); const promptBuilder = (data: Record<string, any>) => `Create a lease abstract from these terms: ${data.leaseTerms}`; return (<GeneratorShell id="lease-abstract" title="Lease Abstract" description="Summarize existing leases into digestible abstracts" icon={FileSearch} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.leaseTerms}><div className="space-y-2"><Label>Lease Terms *</Label><Textarea placeholder="Paste or type key lease terms..." value={formData.leaseTerms} onChange={(e) => setFormData({ ...formData, leaseTerms: e.target.value })} rows={8} /></div></GeneratorShell>); };
export default LeaseAbstractGenerator;
