import { useState } from "react"; import { Scale } from "lucide-react"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const SpaceComparisonGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ spaces: "" }); const promptBuilder = (data: Record<string, any>) => `Compare these commercial spaces: ${data.spaces}`; return (<GeneratorShell id="space-comparison" title="Space Comparison" description="Compare multiple spaces side-by-side for tenants" icon={Scale} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.spaces}><div className="space-y-2"><Label>Spaces to Compare *</Label><Textarea placeholder="List each space with address, SF, rent, terms..." value={formData.spaces} onChange={(e) => setFormData({ ...formData, spaces: e.target.value })} rows={6} /></div></GeneratorShell>); };
export default SpaceComparisonGenerator;
