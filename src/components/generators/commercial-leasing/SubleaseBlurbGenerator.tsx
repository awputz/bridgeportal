import { useState } from "react"; import { Building } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const SubleaseBlurbGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ property: "", details: "" }); const promptBuilder = (data: Record<string, any>) => `Write sublease marketing blurb for ${data.property}. Details: ${data.details}`; return (<GeneratorShell id="sublease-blurb" title="Sublease Blurb" description="Quick sublease marketing copy and descriptions" icon={Building} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.property}><div className="space-y-4"><div className="space-y-2"><Label>Property *</Label><Input value={formData.property} onChange={(e) => setFormData({ ...formData, property: e.target.value })} /></div><div className="space-y-2"><Label>Space Details</Label><Textarea value={formData.details} onChange={(e) => setFormData({ ...formData, details: e.target.value })} rows={4} /></div></div></GeneratorShell>); };
export default SubleaseBlurbGenerator;
