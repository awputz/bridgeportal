import { useState } from "react"; import { Calendar } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const TourConfirmationGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ clientName: "", properties: "", dateTime: "" }); const promptBuilder = (data: Record<string, any>) => `Write tour confirmation for ${data.clientName}. Properties: ${data.properties}. Date/Time: ${data.dateTime}`; return (<GeneratorShell id="tour-confirmation" title="Tour Confirmation" description="Professional tour scheduling and confirmation emails" icon={Calendar} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!(formData.clientName && formData.properties)}><div className="space-y-4"><div className="space-y-2"><Label>Client Name *</Label><Input value={formData.clientName} onChange={(e) => setFormData({ ...formData, clientName: e.target.value })} /></div><div className="space-y-2"><Label>Properties *</Label><Input value={formData.properties} onChange={(e) => setFormData({ ...formData, properties: e.target.value })} /></div><div className="space-y-2"><Label>Date/Time</Label><Input value={formData.dateTime} onChange={(e) => setFormData({ ...formData, dateTime: e.target.value })} /></div></div></GeneratorShell>); };
export default TourConfirmationGenerator;
