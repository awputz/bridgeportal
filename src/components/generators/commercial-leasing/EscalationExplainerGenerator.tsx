import { useState } from "react"; import { TrendingUp } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const EscalationExplainerGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ escalationType: "", details: "" }); const promptBuilder = (data: Record<string, any>) => `Explain rent escalations to client. Type: ${data.escalationType}. Details: ${data.details}`; return (<GeneratorShell id="escalation-explainer" title="Escalation Explainer" description="Client education on rent escalations and increases" icon={TrendingUp} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.escalationType}><div className="space-y-4"><div className="space-y-2"><Label>Escalation Type *</Label><Input placeholder="Fixed %, CPI, Fair Market..." value={formData.escalationType} onChange={(e) => setFormData({ ...formData, escalationType: e.target.value })} /></div><div className="space-y-2"><Label>Lease Details</Label><Input value={formData.details} onChange={(e) => setFormData({ ...formData, details: e.target.value })} /></div></div></GeneratorShell>); };
export default EscalationExplainerGenerator;
