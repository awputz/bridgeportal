import { useState } from "react"; import { Settings } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Textarea } from "@/components/ui/textarea"; import { GeneratorShell } from "../GeneratorShell";
interface Props { onBack: () => void; }
export const TIScopeGenerator = ({ onBack }: Props) => { const [formData, setFormData] = useState({ space: "", requirements: "" }); const promptBuilder = (data: Record<string, any>) => `Create TI scope for ${data.space}. Requirements: ${data.requirements}`; return (<GeneratorShell id="ti-scope" title="TI Scope Generator" description="Tenant improvement scope descriptions and budgets" icon={Settings} onBack={onBack} promptBuilder={promptBuilder} formData={formData} isFormValid={!!formData.space}><div className="space-y-4"><div className="space-y-2"><Label>Space Details *</Label><Input value={formData.space} onChange={(e) => setFormData({ ...formData, space: e.target.value })} /></div><div className="space-y-2"><Label>TI Requirements</Label><Textarea value={formData.requirements} onChange={(e) => setFormData({ ...formData, requirements: e.target.value })} rows={4} /></div></div></GeneratorShell>); };
export default TIScopeGenerator;
